version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: manage_service_backend
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      PORT: "5000"
      DB_HOST: "host.docker.internal"
      DB_PORT: "3306"
      DB_USER: "root"
      DB_PASSWORD: "" # Empty password as per your local backend/.env
      DB_NAME: "manage_service_db"
      JWT_SECRET: "dew23ijkek9ej2nj3n29dujdnioed28i2"
      SMTP_HOST: "smtp.gmail.com"
      SMTP_PORT: "587"
      SMTP_SECURE: "false"
      SMTP_USER: "chaminduirosh927@gmail.com"
      SMTP_PASS: "uzwy asgb ramx zjzk"
      SMTP_FROM_NAME: "Manage Service"
      COMPANY_NAME: "Earrow"
    extra_hosts:
      - "host.docker.internal:host-gateway"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: manage_service_frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    depends_on:
      - backend
